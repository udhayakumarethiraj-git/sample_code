version: 0.2

phases:
  build:
    commands:
      - echo "Copying static website files to artifact directory..."
      - mkdir -p artifacts
      - rsync -av --exclude='artifacts' ./ artifacts/


artifacts:
  files:
    - '**/*'
  base-directory: artifacts
  discard-paths: no

